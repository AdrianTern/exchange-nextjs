import type { Metadata } from 'next';
import { Inter } from 'next/font/google';
import './globals.css';
import Providers from './providers';

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: 'Exchange | Professional Currency Intelligence',
  description: 'Real-time, precision currency conversion and market intelligence. Monitor global exchange rates with professional-grade accuracy.',
  openGraph: {
    title: 'Exchange | Professional Currency Intelligence',
    description: 'Real-time, precision currency conversion and market intelligence.',
    type: 'website',
  },
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <Providers>{children}</Providers>
      </body>
    </html>
  );
}
